<%= form_for [topic, post, post.comments.new] do |f| %>
		<%= f.label 'Post new comment' %>
		<%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter post body" %>
	<div class="form-group">
		<%= f.submit "Post comment", class: 'btn btn-success'%>
	</div>
<% end %>
